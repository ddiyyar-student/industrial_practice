if you want to test it just install rqeuirements (requirements.txt) then run app.py

1. install requirements (pip install -r requirements.txt)
2. run "app.py"
3. open browser and search: localhost:5000
